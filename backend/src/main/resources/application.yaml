spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/webblog
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
